#if !defined(SERIES_H)
#define SERIES_H

#include <string>
#include <vector>

using namespace std;

namespace series {
vector<string> slice(string s, size_t l);
}  // namespace series

#endif  // SERIES_H